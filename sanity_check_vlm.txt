# configs/sanity_check_vlm.yaml

data:
  processed_dir: 'data/processed'
  train_csv: 'train_micro.csv'  # <-- CHANGE
  val_csv: 'val_micro.csv'    # <-- CHANGE
  test_csv: 'test.csv'
  mappings: 'attribute_mappings.json'
  image_dir: 'data/raw/Images' 

model:
  vision_model_name: 'google/vit-base-patch16-224-in21k'
  text_model_name: 'gpt2'

training:
  epochs: 2                     # <-- CHANGE
  batch_size: 16
  learning_rate: 1e-4
  device: 'cuda'
  log_image_freq: 1             # <-- CHANGE (Log every epoch to see results faster)
  weight_decay: 1e-2

wandb:
  project: 'multimodal-product-lister'

output_dir: 'results/'
experiment_name: 'sanity_check_vlm' # <-- CHANGE